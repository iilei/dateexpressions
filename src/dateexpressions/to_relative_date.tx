Program:
  now=Now?
  statements*=Statement
;

Statement:
  DeltaStatement | FloorStatement
;

Now:
  timezone = /now(?:\s*\((?:(?:timezone)=)?(?P<timezone>[^\=)]+)\))?/
;

DeltaStatement: sign=OperationSign value=INT unit=FixedUnit;

OperationSign: Add | Sub;

Add: '+';
Sub: '-';

FixedUnit: /([smhdwy])/;
FloorUnit: /([smhdwyM])/;

FloorStatement: '/' unit=FloorUnit;

Comment:
  /\/\*.*\*\//
;
